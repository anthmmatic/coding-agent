name: "Copilot Setup Steps"

on:
  workflow_dispatch:

jobs:
  copilot-setup-steps:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - run: |
          VALUE1="${{ secrets.SECRET1 }}" >> "$GITHUB_ENV"
          VALUE2="${{ vars.VAR123 }}" >> "$GITHUB_ENV"
      - run: |
          echo ${VALUE1}
          echo ${VALUE2}
